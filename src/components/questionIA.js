import { useState } from "react"
import { View, Text } from "react-native"

const QuestionIA = () => {
    const [conversation, setConversations] = useState([])

    return(
        <View>
        </View>
    )
}

export default QuestionIA